service readflag
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    server = /home/readflag/run.sh
    user = readflag
    port = 40001
    flags = REUSE
    bind = 0.0.0.0
    wait = no
}
